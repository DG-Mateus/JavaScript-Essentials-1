let numeroa = 20;
let numerob = 5;
let numeroc = 0;

numeroc = numeroa + numerob;
console.log(numeroc);
numeroc =  numeroa - numerob;
console.log(numeroc);
numeroc = numeroa * numerob;
console.log(numeroc);
numeroc = numeroa/numerob;
console.log(numeroc);
numeroc = numeroa%numerob;
console.log(numeroc);